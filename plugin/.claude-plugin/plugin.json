{
  "name": "agent-tasks",
  "version": "0.3.0",
  "description": "Board 5 cột human-first (Backlog · Working · Needs you · In review · Ready to merge) cho nhiều phiên agent trên GitLab: chống trùng task bằng claim có TTL trên git refs, hiện ai/máy/agent đang làm, bắt agent viết hướng dẫn QC, nợ kỹ thuật thành issue riêng.",
  "author": { "name": "nexa" },
  "keywords": ["gitlab", "work-items", "multi-session", "claim", "agent-loop"],
  "mcpServers": {
    "agent-tasks": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.mjs"]
    }
  }
}
