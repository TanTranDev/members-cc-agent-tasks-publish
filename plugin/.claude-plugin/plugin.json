{
  "name": "agent-tasks",
  "version": "0.2.1",
  "description": "Điều phối công việc cho nhiều phiên agent qua GitLab work items: chống trùng task bằng claim có TTL trên git refs, đưa brief/spec/changelog thành item, và giữ nhãn/trạng thái đủ để agent tự quyết đúng.",
  "author": { "name": "nexa" },
  "keywords": ["gitlab", "work-items", "multi-session", "claim", "agent-loop"],
  "mcpServers": {
    "agent-tasks": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.mjs"]
    }
  }
}
